apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: openclaw-ingress
spec:
  rules:
  - host: yourdomain.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: openclaw-service
            port:
              number: 80
